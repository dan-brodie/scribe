Load local vendor documentation for a dependency.

Usage: /vendor <name>   e.g. /vendor fluidaudio

Accepted names: fluidaudio, mlx-swift-lm, grdb, core-audio-taps, eventkit

Read the file `docs/vendor/$ARGUMENTS.md` and output it in full.

If no argument is given, list all vendor doc files.
